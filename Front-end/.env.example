# API base URL (defaults to /api — proxied to FastAPI in dev via vite.config.ts)
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: override Vite proxy target
# VITE_PROXY_TARGET=http://127.0.0.1:8000
